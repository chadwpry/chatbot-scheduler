import logger from "@/lib/logger";

export const sendMessage = async (message: string) => {
    logger.info(`TODO: setup communication channel via twilio/SMS or other system`);
    logger.info(message);
}